# Team override — slug-folder artifact layout.
# See _bmad/custom/artifact-layout.md

[workflow]
persistent_facts = [
  "file:{project-root}/_bmad/custom/artifact-layout.md",
  "All planning artifacts: {planning_artifacts}/{slug}/. All implementation artifacts: {implementation_artifacts}/{slug}/. Resolve slug per artifact-layout.md.",
]
