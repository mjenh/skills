# Team override — slug-folder artifact layout.
# See _bmad/custom/artifact-layout.md

[workflow]
persistent_facts = [
  "file:{project-root}/_bmad/custom/artifact-layout.md",
  "sprint_status = {planning_artifacts}/{slug}/sprint-status.yaml; default_output_file = {implementation_artifacts}/{slug}/{{story_key}}.md; epics at {planning_artifacts}/{slug}/{slug}-epics.md.",
]
