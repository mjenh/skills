# Team override — slug-folder artifact layout.
# See _bmad/custom/artifact-layout.md

[workflow]
persistent_facts = [
  "file:{project-root}/_bmad/custom/artifact-layout.md",
  "sprint_status = {planning_artifacts}/{slug}/sprint-status.yaml; spec_file = {implementation_artifacts}/{slug}/spec-{slug}.md. Resolve product slug before routing.",
]
